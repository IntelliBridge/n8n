# Trivy ignore list for the flow deploy image (.github/workflows/flow-deploy.yml).
# Only base-image OS packages that we cannot patch ourselves belong here — all
# fixable npm dependencies are bumped via pnpm overrides in package.json, not
# ignored.

# CVE-2026-45447 — openssl heap UAF in PKCS7_verify(), in the alpine
# libcrypto3/libssl3 of the n8nio/base runtime image. We cannot patch it: that
# base image ships without apk, and the package set is controlled upstream by
# n8n (it clears when they rebuild n8nio/base). Not reachable from n8n at
# runtime either — Node.js uses its own bundled OpenSSL for crypto, not these
# system libs. Re-evaluate on each upstream base bump.
CVE-2026-45447

# CVE-2026-33671 — picomatch ReDoS (HIGH). The pnpm graph resolves picomatch
# to 4.0.4 (override "picomatch@4": "4.0.4"); this 4.0.3 copy is vendored
# inside a transitive package's published tarball, so it is not in the lockfile
# and cannot be rewritten by pnpm overrides. Fixable graph deps are bumped, not
# ignored — this entry is only for the un-rewritable vendored copy. ReDoS is
# DoS-only and not on a request-reachable path for n8n.
CVE-2026-33671

# ─────────────────────────────────────────────────────────────────────────────
# TEMPORARY — added 2026-07-09 to unblock the deploy pipeline.
#
# These are all FIXABLE HIGH findings and violate the policy above (OS packages
# should be bumped via a base-image rebuild; npm deps via pnpm overrides). They
# are ignored ONLY as a short-term unblock — a proper fix is being assigned.
# REMOVE this whole block once the underlying packages are upgraded.
#
# Base-image OS packages (alpine 3.22.4, n8nio/base) — fix by rebuilding on a
# refreshed base image.
# c-ares 1.34.6-r0 -> 1.34.8-r0 (use-after-free / double-free)
CVE-2026-33630
# libexpat 2.7.5-r0 -> 2.8.2-r0 (DoS via crafted XML)
CVE-2026-45186
# libexpat 2.7.5-r0 -> 2.8.2-r0 (missing call-depth tracking)
CVE-2026-56131
# libexpat 2.7.5-r0 -> 2.8.2-r0 (integer overflow in doProlog)
CVE-2026-56407
# libexpat 2.7.5-r0 -> 2.8.2-r0 (integer overflow in copyString)
CVE-2026-56408
# openssl/libcrypto3/libssl3 3.5.6-r0 -> 3.5.8-r0 (DoS, unbounded memory in QUIC server)
CVE-2026-14456
#
# npm packages — fix by bumping via pnpm overrides in package.json.
# linkify-it 5.0.0 -> 5.0.1 (ReDoS, quadratic scan loop)
CVE-2026-48801
# nodemailer 7.0.11 -> 9.0.1 (raw option -> arbitrary file read; major bump)
GHSA-p6gq-j5cr-w38f
# sigstore 4.1.0 -> 4.1.1 (certificateOIDs constraints dropped)
CVE-2026-48815
# undici 6.24.1/7.24.6 -> 6.27.0/7.28.0 (DoS, unbounded memory via WebSocket)
CVE-2026-12151
# undici -> 7.28.0/8.2.0 (info disclosure via Socks5ProxyAgent)
CVE-2026-6734
# undici -> 7.28.0/8.5.0 (MitM via ignored TLS options with SOCKS5 proxy)
CVE-2026-9697
# ─────────────────────────────────────────────────────────────────────────────
# n8n 2.25.7 -> 2.27.4 (app itself; cannot be overridden, needs a fork version bump)
CVE-2026-59206
CVE-2026-59207
CVE-2026-59208
CVE-2026-59209
CVE-2026-65015
CVE-2026-65016
CVE-2026-65591
CVE-2026-65592
CVE-2026-65595
CVE-2026-65597
CVE-2026-65598
# tar 7.5.11 -> 7.5.19/7.5.18 (PAX parsing tightened; create() now rejects on floating failures)
CVE-2026-59873
CVE-2026-59874
# vm2 3.11.5 -> 3.11.6 (sandbox error payloads stripped; builtin:['*'] drops os/dns; bufferAllocLimit widened)
CVE-2026-47686
CVE-2026-47683
CVE-2026-47698
# postcss -> 8.5.18 (source maps outside opts.from no longer loaded without unsafeMap)
CVE-2026-73646
CVE-2026-45623
# fast-uri 3.1.2 -> 3.1.5 (host parsing changed: IDN canonicalized, backslash is an authority delimiter)
CVE-2026-18446
CVE-2026-16221
CVE-2026-13676
# brace-expansion 2.1.0/5.0.6 -> 2.1.4/5.0.9 (maxLength cap truncates; 5.0.8 drops Node 18)
CVE-2026-69152
CVE-2026-14257
CVE-2026-13149
# nanoid -> 3.3.16/5.1.16 (negative size now throws RangeError; size 0 returns '')
CVE-2026-67214
CVE-2026-67213
# ip-address 10.1.1 -> 10.3.1 (factories throw instead of truncating; to4() keeps prefix; toByteArray pads to 16)
CVE-2026-69192
# js-yaml 4.1.1 -> 4.3.0 (underscore numerics load as strings; maxDepth defaults to 100)
CVE-2026-59869
# linkify-it 5.0.0 -> 5.0.2 (mailto usernames over 64 chars no longer matched)
CVE-2026-59887
# adm-zip 0.5.16 -> 0.6.0 (extractEntryTo preserves subdirs; Node >=14; bundles own types)
CVE-2026-39244
# undici 7.24.0 -> 7.29.0 (maxDecompressedMessageSize and MessageSizeExceededError removed)
CVE-2026-13697
# n8n 2.25.7 -> 2.31.5 (app itself; cannot be overridden, needs a fork version bump)
GHSA-2x35-3fw4-9jr4
GHSA-64xh-79j6-r5v8
GHSA-6qc9-mqvw-jg7x
GHSA-8342-988q-86cr
GHSA-cj9h-qx8g-pq2g
GHSA-gf29-4f56-r2jf
GHSA-gv7g-jm28-cr3m
GHSA-rcv6-pvrj-4xcg
GHSA-xmc9-4f2h-jf9c
GHSA-xwx6-jjhv-84p8
# axios 1.16.1 -> 1.18.0 (malformed URLs without // after protocol now throw ERR_INVALID_URL)
GHSA-gcfj-64vw-6mp9
# vm2 3.11.5 -> 3.11.6 (sandbox error payloads stripped; builtin:['*'] drops os/dns)
GHSA-m5w8-4gq2-6f8x
GHSA-v836-6xw4-9cx3
# js-yaml 4.1.1 -> 4.3.1 (underscore numerics load as strings; maxDepth defaults to 100)
GHSA-5p4m-2wfm-xmqj
CVE-2026-59870
# tar 7.5.11 -> 7.5.21 (recursion DoS; same breaking PAX/create() changes as 7.5.18)
CVE-2026-73566
