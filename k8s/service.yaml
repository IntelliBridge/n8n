apiVersion: v1
kind: Service
metadata:
  name: flow-service
  namespace: flow
  labels:
    app: flow
spec:
  selector:
    app: flow
  ports:
  - port: 80
    targetPort: 5678
    protocol: TCP
    name: http
  type: ClusterIP